[package]
name = "untitled-rustgame"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.19.0", features = ["bevy_settings"] }
toml = "1.1.2"
serde = { version = "1.0.228", features = ["derive"] }
noise = "0.9.0"

[profile.dev.package."*"]
opt-level = 3
